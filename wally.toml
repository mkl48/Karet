[package]
name = "kr3ative/karet"
version = "0.1.0"
description = "An in-game command framework and terminal for Roblox"
license = "MIT"
authors = ["kr3ative"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
Switch = "ker/switch@0.1.0"        # input detection — client-only (errors on the server)
Substance = "ker/substance@0.1.0"  # networking — client/server transport
